<!DOCTYPE html>
<html>
<head>
    <title>Node News || Welcome <% if(!user) { %>guest<% } else if(user) { %><%= user.username %><% }%></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
<div class="header">
    <% if(!user) { %>
    <p><a href="/register">Register</a> or <a href="/login">Login</a></p>
    <% } else if(user) { %>
    <p>Welcome <%= user.username %> || <a href="/logout">Logout</a></p>
    <% } %>
</div>